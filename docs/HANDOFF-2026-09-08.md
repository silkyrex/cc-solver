# Handoff — session of 2026-09-08 (PT)

Written 2026-09-08 ~7:30 PM PT. Everything below is on `main` unless it says otherwise.
Assume the reader remembers nothing about this session.

## Where things stand

`main` is `cf5c139`. **49 tests pass.** Eight PRs merged tonight, all squashed, all branches deleted.

The session started as a review of `grok/build-2` against `main` and turned into eight fixes,
two of which were live bugs on positions Ray actually holds.

## What shipped, in order

| PR | What | Why it mattered |
|---|---|---|
| #1 | Anchored the exclusion matcher | `Ultra` matched Ultragenyx (RARE), `Bear` matched RBC Bearings (ROLL), `Daily` matched Daily Journal (DJCO). Real momentum candidates were deleted before reaching a door check. |
| #2 | Union Notion patterns with the built-in list | Found by testing #1 against the live Notion page: TQQQ leaked, because the page writes `Ultra` and means the family, and boundary matching does not. |
| #3 | Side-aware exit doors | **Live bug.** TJX is short and its stop computed at 92.0, *below* the price. That stop exits at a profit and leaves the real risk unprotected. |
| #4 | Exit door binds at entry, graduates | **Ray caught this one.** A position entered below the 21 EMA was born inside its own mandatory exit — verified at `closes_against_21ema=27` on the staging bar. It would have hit every relaunch entry. |
| #5 | 12-hour `pt_time` | **Live crash.** The task prompts moved to a 12-hour clock the same night; `minutes_since_open` raised `ValueError` on `"11:50 AM"`. `take_action` would have died at the 11:50 AM slot. |
| #6 | Provisional exits, timestamps, fuller `held_row` | MOC deadline is 12:45 PM, close is 1:00 PM, `position_monitor` runs 1:10 PM. Exits were knowable only *after* Ray could act. |
| #7 | `confirm_pass` returns `pending_exits` | #6 built the read; nothing ran early enough to use it. 12:35 PM is the last actionable slot. |
| #8 | One PT clock + a time check before anything time-dependent | 11:50 AM PT is 18:50 UTC. A UTC time in `pt_time` was 420 minutes wrong and produced normal-looking verdicts. |

## Rulings Ray made tonight

All recorded in `docs/reviews/2026-09-08-build-2-review.md` and in the code where they bite.

1. **Same-day scan hits may stage.** The entry door is reclaim day 1 or 2, so a one-session lag eats the window.
2. **Pump threshold: sub-$10** on >20x RVOL.
3. **Slow sto = SMA(3) of fast %K**, not raw. Raw dips under 20 far more often and would arm the second entry door on setups Ray never sees.
4. **Solver emits all three size tiers; Ray picks.**
5. **Cluster themes come from Notion at run time**, not a committed file.
6. **21 EMA second consecutive close = mandatory exit. 4 EMA = warning.** Deep break (>4% through the 4 EMA) also mandatory. **This was a direct ruling, NOT a backtest result** — a patch cited a "Build 4 backtest" that does not exist in any branch. Never cite one.
7. **The exit door binds at entry and graduates** (see PR #4).
8. **Every date carries a time**, and anything Ray reads is a 12-hour clock.

## Positions database (new, 2026-09-09)

`collection://10461ddd-3dcc-42c9-9dbe-16b2e21600b5` — https://app.notion.com/p/323b5220b221499397e80bce89495234

Replaces parsing the Trader Handoff "Open positions" free text, which every task re-parsed daily and
which silently defaulted a position's exit door on one malformed character.

- **EOD owns it.** Creates rows on entry fills, closes them on exit fills, reconciles against IBKR.
- **Position monitor reads it** straight into `held.json`. No parsing.
- **Take-action never writes it.** It carries the `held_row` block forward in `STAGED_JSON`.
- The Handoff text is now a human-readable mirror. Nothing reads it back as data.

Chosen over SQLite because Cowork task containers are ephemeral: a local `.db` vanishes every run,
and Notion gives typed rows, concurrent writes, and a UI Ray can fix by hand.

**It is empty.** The first `position_monitor` run backfills open positions from IBKR and flags them
🟡 with incomplete entry context. That is expected, once.

## The division of labour that is easy to get wrong

The task prompts say it out loud: *"where this prompt and the repo differ, the repo wins."*

- **What the solver OUTPUTS** — repo controls it. Adding a field needs no Notion edit.
- **What the task writes IN** — the Notion prompt alone controls it. Requiring a new input field
  always needs a prompt edit.

Missing that split is exactly how the 12-hour `pt_time` change crashed `take_action` (PR #5).

## Next actions

1. **Move confirm pass 12:35 PM → 12:25 PM.** Ray asked; nothing has been changed yet. 17 places:
   9 on the confirm-pass Notion page (title, `Fire Time` property, cron, `What It Does`, and 5 body
   mentions including the early-close shifted slot `9:35 AM` → `9:25 AM` and the derived phrase
   "10 minutes before" → "20 minutes"), 2 on the take-action page, 6 in the repo
   (`ccsolver/calendar.py:15` is the one that drives behaviour, plus `cli.py` ×2, `README.md`,
   `tests/test_cli_confirm_pass.py` ×2). **The Notion cron is documentation only** — trigger
   `trig_016TQ6om8cHqgYnDrfzvgV1C` must be re-timed to `25 19 * * 1-5` separately, or the page will
   say 12:25 while the task fires at 12:35.
   Also check position-monitor and EOD pages for `12:35`; not verified.
2. **Update the confirm-pass Notion prompt for `pending_exits`.** PR #7 shipped the field; the
   prompt still only knows about `flips`. It needs held.json + positions.json in its inputs and a
   push path for pending exits.
3. **Update the position-monitor prompt** for `entry_datetime` (it still says `entry_date`) and add
   `quotes.json` so the provisional read runs there too.
4. **Verify the live Notion Exclusion List patterns still fire** under the two-token rule. PR #2
   fixed the known leak, but only the C1 examples on the page were checked.
5. **Reclaim day 1-to-5 test.** Ray wants to know whether the day-1/day-2 cutoff is real edge or an
   untested assumption. `reclaim_day_at_entry` now records the bucketing key on every entry, so the
   data starts accumulating from today. Needs a backtest harness and a bar corpus, neither of which
   exists.
6. **`grok/build-2` is still open.** Nothing in it survives except two ports worth taking:
   `backtest/harness.py` (a per-day state dumper, not a backtest — see the review) and
   `persistence.py` (SHA-256 manifests). Retire the branch after.

## Things deliberately NOT done

- **Regime is not in `held_row`.** It reads like an obvious field, but no task prompt supplies it.
  Inventing a source for a sizing input is a guess that goes unnoticed.
- **The backtest harness was NOT ported.** It was queued to settle the exit door, which Ray ruled
  directly instead. It is a per-day state dumper: no position simulation, no policy comparison, no
  aggregation, output truncated to 50 rows.
- **No bar corpus was pulled.** That is the real blocker for every remaining rule question.

## Traps worth knowing

- **A test that passes for the wrong reason.** Two attempts at the `confirm_pass` pending-exit test
  were green while testing nothing: an uptrend entry binds to the 21 EMA door, and any close far
  enough below the 4 EMA to matter is already a deep break, i.e. *confirmed* rather than pending.
  Use a relaunch-shaped fixture. The reasoning is in the fixture docstring.
- **A provisional bar must never graduate a position.** Graduation is one-way, so an intraday pop
  over the 21 EMA that fades by the close would loosen the leash permanently. The live bar carries
  `provisional=True` and graduation skips it.
- **Never guess a side.** A wrong side inverts the stop onto the profitable side of price. Both
  `position_monitor` and `confirm_pass` return `SIDE UNKNOWN` and push rather than defaulting.
- **The exclusion matcher still needs both halves.** A leverage word on a word boundary AND an
  issuer token. Compound forms (`UltraShort`, `UltraProShort`) are enumerated explicitly, because
  boundary matching means `Ultra` no longer matches inside `UltraPro`.

## Environment

`gortex` is broken machine-wide: a stale `~/.cache/gortex/daemon.pid` points at PID 724, which the
OS recycled into an unrelated system process, so the daemon refuses to start and its MCP server
never connects. Its PreToolUse hook then blocks `Read` on every `.py` under `~/projects` while
offering only tools that are unavailable. Fix: `rm ~/.cache/gortex/daemon.pid && gortex daemon start --detach`,
then restart the session. Ray said to work around it for this session.
